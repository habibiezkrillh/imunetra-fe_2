import 'package:flutter/material.dart';

/// Warna utama aplikasi (biru)
const Color kPrimaryColor = Color(0xFF3B6BFD);

/// Warna ikon/tab yang tidak aktif (abu-abu)
const Color kInactiveColor = Color(0xFF8D8D8D);

/// Default padding horizontal
const double kDefaultPadding = 16.0;

/// Nama font default (jika pakai Poppins)
const String kFontFamily = 'Poppins';
